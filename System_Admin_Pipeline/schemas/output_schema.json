{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": [
        "success",
        "failure",
        "pending_review"
      ],
      "description": "The outcome of the orchestration pipeline."
    },
    "instances_affected": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of EC2 instances that were started or stopped."
    },
    "active_connections": {
      "type": "integer",
      "description": "The current number of active connections detected by pyCount.py."
    },
    "message": {
      "type": "string",
      "description": "Detailed execution summary or error message."
    }
  },
  "required": [
    "status",
    "message"
  ]
}