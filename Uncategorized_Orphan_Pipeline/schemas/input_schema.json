{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "target_directory": {
      "type": "string",
      "description": "The local directory path to scan for orphaned files (e.g., ~/.gemini/antigravity/orphans/)."
    },
    "scan_mode": {
      "type": "string",
      "enum": [
        "dry_run",
        "categorize",
        "full_integration"
      ],
      "description": "The operational mode for the pipeline."
    },
    "orphan_file_paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional list of specific orphaned file paths to process."
    }
  },
  "required": [
    "target_directory",
    "scan_mode"
  ]
}