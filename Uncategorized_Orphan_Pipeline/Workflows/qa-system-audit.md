---
description: Trigger the automated QA system audit to execute rigorous health checks
---

# QA System Audit Workflow

// turbo-all

This workflow automates the execution of the full AROS QA Engineering test suite.

# Prerequisites
Ensure you are running in an environment with Python 3.10+ and `pytest` installed.

## Fully Autonomous QA Testing Swarm

> [!WARNING]
> This test suite triggers a completely autonomous multi-agent swarm matrix (`qa_engineering/run_all_tests.py`).
> 1. The Swarm will unrestricted mock biological data prior to runtime.
> 2. Pytest will evaluate deterministic structural and non-deterministic logic faults.
> 3. If a test fails, the Swarm orchestrator will lock into a `MAX_RETRIES = 25` geometric self-healing loop. It will consume **API tokens rapidly** during failure, attempting to read `SPEC.md` and autonomously rewrite the test suite or core OS backend until everything succeeds.

## IDE Agent Directives (Execution Plan)

As the IDE AI Agent reading this workflow, you MUST execute the following exact steps autonomously:

### Step 1: Run and Wait (Pause)
1. Run the testing entry point exactly as written below using your `run_command` tool.
2. You MUST check the command status and wait asynchronously to ensure you **PAUSE** the IDE interface and completely block further action until the python script exits.

```bash
PYTHONPATH=${PWD} python3 ./qa_engineering/run_all_tests.py
```

### Step 2: Extract Telemetry Logs
Once Step 1 has terminated, use your tools to inspect the `[WORKSPACE_ROOT]/qa_engineering/test_logs/` directory. Read the `.log` files generated by the Swarm.

### Step 3: Synthesize Testing Report
Finally, use your filesystem tools to render a comprehensive markdown file at `artifacts/qa_report.md`. This report must synthesize:
- The total execution time and MAX_RETRIES taken.
- The bugs caught by Pytest.
- The Agentic logic used to patch the source code (extracted from the log histories).
- The final state of the QA process.
